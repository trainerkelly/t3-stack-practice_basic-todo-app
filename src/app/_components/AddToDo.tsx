export default function AddToDo() {
  return (
    <div>
      <label htmlFor="task">
        <h2>Add Task</h2>
      </label>
      <input id="task" />
      <button>Add Task</button>
    </div>
  );
}
